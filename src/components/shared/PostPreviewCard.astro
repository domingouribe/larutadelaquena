---
const { post } = Astro.props;
import Button from "../shared/Button.astro";
---

<article class="Post">
	<a href={post.url}>
		<h3 class="Post-title">
			{post.frontmatter.title}
		</h3>
		<img
			src={post.frontmatter.image.url}
			alt={post.frontmatter.image.alt}
			width={300}
			class="Post-image"
		/>
		<p class="Post-description">
			{post.frontmatter.description}
		</p>

		<Button
			link={`${post.url}`}
			buttonText="Leer"
			classes="Button Button-sm Button-primary"
		/>
	</a>
</article>

<style lang="scss">
	.Post {
		//width: 200px;
		padding: 1em;
		background-color: var(--uiGrayMedium);
		gap: 1em;
		border-radius: 0.5em;

		h3,
		p {
			margin: 0;
		}

		&-image {
			width: 100%;
			object-fit: cover;
			aspect-ratio: 16/9;
		}

		a {
			text-decoration: none;
			:hover {
				text-decoration: underline;
			}
		}
	}
</style>
