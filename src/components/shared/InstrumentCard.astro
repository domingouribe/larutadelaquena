---
import type { Instrument } from "../../types/types";
interface Props {
  item: Instrument;
}
const { item } = Astro.props;
---

<div class="InstrumentCard">
  <h2>{item.title}</h2>
  <p>{item.price}</p>
</div>
